# Programming Principles in StudentAccounting Project

Цей документ описує основні принципи програмування, яких я дотримуюся під час розробки проєкту **StudentAccounting**.  
Наводжу принципи з прикладами рядків коду з репозиторію.

---

## 1. Принцип єдиної відповідальності (Single Responsibility Principle)

Кожен клас та метод має **чітке завдання**.

### Приклад 1: Основне вікно застосунку — `MainWindow.xaml.cs`

Метод `LoadStudents()` завантажує студентів із бази даних, **без логіки UI чи авторизації**:

🔗 https://github.com/ipz243kmo/student-accounting-system/blob/main/StudentAccounting/StudentAccounting/MainWindow.xaml.cs#L32-L78

### Приклад 2: Обробка входу студента

Метод `StudentLoginButton_Click` відповідає лише за **перевірку вибору та відкриття відповідного вікна**:

🔗 https://github.com/ipz243kmo/student-accounting-system/blob/main/StudentAccounting/StudentAccounting/MainWindow.xaml.cs#L118-L137

---

## 2. Відокремлення UI від логіки доступу до даних

Усі операції з SQL Server реалізовані в окремих методах (`LoadStudents`, `LoadTeachers`, тощо) без змішування з деклараціями UI компонентів.

### Приклад: Завантаження викладачів

Метод `LoadTeachers()` читає дані з таблиці `Professors`:

🔗 https://github.com/ipz243kmo/student-accounting-system/blob/main/StudentAccounting/StudentAccounting/Admin.xaml.cs#L87-L129

Це відокремлює **діяльність з DB** від **відображення в UI**.

---

## 3. Використання моделей (Model classes)

Класи для зберігання даних:

- `Teacher`
- `Student`
- `Course`
- `FacultyItem`

Ці класи використовуються для представлення даних, прочитаних з БД, що покращує структуру коду.

### Приклад: клас для викладача

🔗 https://github.com/ipz243kmo/student-accounting-system/blob/main/StudentAccounting/StudentAccounting/Admin.xaml.cs#L50-L74

---

## 4. DRY (Don’t Repeat Yourself)

Повторювані дії **виносяться в окремі методи**, а не дублюються:

🔹 Підключення до БД:
```csharp
using SqlConnection conn = new SqlConnection(connectionString);
